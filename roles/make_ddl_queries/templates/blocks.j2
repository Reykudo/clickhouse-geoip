 CREATE OR REPLACE DICTIONARY {{ CLICKHOUSE_GEOIP_DB }}.`GeoLite2-City-Blocks-{{ IP_STANDART }}`
(
    `network` String,
    `geoname_id` UInt32,
    `registered_country_geoname_id` UInt32,
    `represented_country_geoname_id` UInt32,
    `is_anonymous_proxy` UInt8,
    `is_satellite_provider` UInt8,
    `postal_code` String,
    `latitude` Float32,
    `longitude` Float32,
    `accuracy_radius` String
)
PRIMARY KEY network
SOURCE(FILE(PATH '{{ CLICKHOUSE_GEOIP_DIR_NAME }}/GeoLite2-City-Blocks-{{ IP_STANDART }}.csv' FORMAT 'CSVWithNames'))
LIFETIME(MIN 0 MAX 300)
LAYOUT(IP_TRIE())
SETTINGS(format_csv_allow_single_quotes = 0)